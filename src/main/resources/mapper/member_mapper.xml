<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bigmoim.module.member.repository.MemberRepository">

    <sql id="fullColumn">
        memberId,
        memberPw,
        memberName,
        memberTel,
        memberBirth,
        memberImg,
        memberProfile,
        memberGender,
        memberAddr,
        memberJobAddr,
        memberLikeArea,
        categoryNum,
        businessNum,
        taskNum,
        themeNum,
    </sql>

    <select id="findById" resultType="com.bigmoim.module.member.repository.MemberRepository">
        SELECT <include refid="fullColumn"/>
        FROM `member`
        WHERE memberId = #{memberId}
    </select>

    <insert id="insert">
        INSERT INTO `member` (
            memberId,
            memberPw,
            memberName,
            memberTel,
            memberBirth,
            memberImg,
            memberProfile,
            memberGender,
            memberAddr,
            memberJobAddr,
            memberLikeArea,
            categoryNum,
            businessNum,
            taskNum,
            themeNum,
        )
        VALUES (
                   #{memberId},
                   #{memberPw},
                   #{memberName},
                   #{memberTel},
                   #{memberBirth},
                   #{memberImg},
                   #{memberProfile},
                   #{memberGender},
                   #{memberAddr},
                   #{memberJobAddr},
                   #{memberLikeArea},
                   #{categoryNum},
                   #{businessNum},
                   #{taskNum},
                   #{themeNum}
               )
    </insert>

</mapper>